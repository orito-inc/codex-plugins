interface:
  display_name: "Implementation Brief"
  short_description: "Turn settled decisions into an implementation brief"
  default_prompt: "Use $implementation-brief to turn the confirmed decisions in this conversation into an implementation brief."
policy:
  allow_implicit_invocation: false
